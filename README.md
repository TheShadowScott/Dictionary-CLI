This service uses the Oxford Dictionary API. There is a required `LoginData.xml` file for credentials.
File setup is as follows:
```xml
<LoginData>
    <AppId>AppId goes here</AppId>
    <AppKey>AppKey goes here</AppKey>
</LoginData>
```